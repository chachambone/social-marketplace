<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="<%= description %>">
    
    <!-- Import map for Lit -->
    <script type="importmap">
        {
            "imports": {
                "lit": "https://cdn.jsdelivr.net/npm/lit@3.2.1/index.js",
                "lit/": "https://cdn.jsdelivr.net/npm/lit@3.2.1/",
                "lit/decorators.js": "https://cdn.jsdelivr.net/npm/lit@3.2.1/decorators.js",
                "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.2.0/lit-html.js",
                "lit-html/": "https://cdn.jsdelivr.net/npm/lit-html@3.2.0/",
                "lit-element/lit-element.js": "https://cdn.jsdelivr.net/npm/lit-element@4.1.0/lit-element.js",
                "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/reactive-element.js",
                "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/"
            }
        }
    </script>
    
    <!-- Tailwind CSS CDN - Pure Tailwind, no custom CSS -->
    <link rel="stylesheet" href="/css/output.css">
    
    <!-- Custom animations -->
    <style>
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        .animate-spin {
            animation: spin 1s linear infinite;
        }
        
        @keyframes slideDown {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes bounce {
            0%, 100% { transform: translateY(0) scale(1) rotate(0deg); }
            50% { transform: translateY(-10px) scale(1.1) rotate(12deg); }
        }
        .animate-slideDown { animation: slideDown 0.3s ease-out; }
        .animate-bounce { animation: bounce 0.5s ease-in-out; }
        .animate-pulse { 
            animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite; 
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        /* Dark mode styles */
        .dark-mode {
            background-color: #1a1a2e;
            color: #eee;
        }
        .dark-mode .bg-white {
            background-color: #16213e !important;
            color: #eee !important;
        }
        .dark-mode .bg-gray-50 {
            background-color: #0f3460 !important;
        }
        .dark-mode .text-gray-900 {
            color: #eee !important;
        }
        .dark-mode .text-gray-600,
        .dark-mode .text-gray-500,
        .dark-mode .text-gray-400 {
            color: #aaa !important;
        }
        .dark-mode .border-gray-100,
        .dark-mode .border-gray-200 {
            border-color: #2a2a4a !important;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">
    <div id="app" class="flex flex-col min-h-screen">
        <!-- The navbar component will be injected here -->
        
        
        
        <main class="flex-1 w-full">
            <img style="width: 100%;" src="https://ke.jumia.is/cms/2026/W15/KE_ONS_BHM_Generic_0426_STB_rvsd.gif" class="w-100"/>
            <marketplace-app></marketplace-app>
        </main>

        <footer class="bg-white border-t border-gray-200 mt-auto">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
                <p class="text-center text-gray-500 text-sm">
                    © <span id="current-year"></span> TradeHive. Buy, sell, and negotiate with confidence.
                </p>
            </div>
        </footer>
    </div>
    
    <!-- Load your compiled app -->
    <script type="module" src="/dist/app.js"></script>
    
    <!-- Dynamic year for footer -->
    <script>
        document.getElementById('current-year').textContent = new Date().getFullYear();
        
        // Listen for dark mode changes to update body class
        window.addEventListener('darkmode-changed', (e) => {
            if (e.detail.isDarkMode) {
                document.body.classList.add('dark-mode');
            } else {
                document.body.classList.remove('dark-mode');
            }
        });
        
        // Check initial dark mode
        const savedDarkMode = localStorage.getItem('bidnest_darkMode');
        if (savedDarkMode === 'true') {
            document.body.classList.add('dark-mode');
        }
    </script>


<!--Start of Tawk.to Script-->
<script type="text/javascript">
var Tawk_API=Tawk_API||{}, Tawk_LoadStart=new Date();
(function(){
var s1=document.createElement("script"),s0=document.getElementsByTagName("script")[0];
s1.async=true;
s1.src='https://embed.tawk.to/69e352573694771c3a8c940b/1jmfvjaan';
s1.charset='UTF-8';
s1.setAttribute('crossorigin','*');
s0.parentNode.insertBefore(s1,s0);
})();
</script>
<!--End of Tawk.to Script-->

</body>
</html>